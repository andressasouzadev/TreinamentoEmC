# Questão 03

Exemplo de um programa básico em C:

```c
#include <stdio.h>

int main(void) {
    printf("Ola, mundo!\n");
    return 0;
}
```

`stdio.h` permite usar o `printf()`. A função `main()` é onde o programa começa e `return 0` indica que terminou normalmente.
