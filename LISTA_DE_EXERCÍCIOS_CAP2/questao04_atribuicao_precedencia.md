# Questão 04 — Operadores de Atribuição Composta e Precedência

## Enunciado
Determine os valores finais, passo a passo:

```c
int a = 1, b = 2, c = 3, d = 4;
a += b + c;
b *= c = d + 2;
d %= a + a + a;
d -= c -= b -= a;
a += b += c += 7;
```

## Resposta
